{
  "description": "Dev Flow checks supported file writes against the active Task Plan before execution.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^apply_patch$",
        "hooks": [
          {
            "type": "command",
            "command": "dev-flow-codex hook pre-tool-use",
            "timeout": 30,
            "statusMessage": "Checking Dev Flow file scope"
          }
        ]
      }
    ]
  }
}
