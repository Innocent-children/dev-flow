# Repository Scripts

Repository-local development validation lives here. `validate-repository.sh` runs only the bounded
Feature 001 checks shared by local development and pull-request CI.

Scripts in this feature do not install host products, launch Codex or DeepSeek, modify user
configuration, build release assets, or publish anything.
