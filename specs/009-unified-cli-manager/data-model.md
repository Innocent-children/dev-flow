# Data Model

This feature does not introduce new database tables or SQLite schema changes.
It interacts with existing data directories and configurations purely at the filesystem level (`fs.rmSync`).

Data disposition: **not-applicable**.
